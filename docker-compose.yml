
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "40000:40000"
    environment:
      - NODE_ENV=production
      - PORT=40000
    volumes:
      - ./server/GD:/app/server/GD:ro
    restart: unless-stopped